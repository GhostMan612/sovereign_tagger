# CURRENT_STATE.md — Sovereign Tagger v2 (post-Phase 27)
> Snapshot as of 2026-09-25 — Phases 0-27 COMPLETE. `flutter analyze --no-pub` → `No issues found!`
>
> **Phase 27 deltas (read before the older map below — rows it contradicts are superseded):**
> - Cross-tab hand-off: `SovereignState.sendToForge(ForgeRequest)` (user-initiated only); `pendingForgePath` is gone. Shared URLs arrive via `SovereignState.pendingGrabberUrl`.
> - New core modules: `title_cleaner`, `lrc`, `tag_io`, `storage_client`, `metadata_sources`, `forge_request`, `playback_fx`, `eq_mapper`, `playlists`, `whisper_model`; widgets `metadata_review_sheet`, `playlist_picker`.
> - Forge/Batch save back over originals via `storage` channel `requestWriteAccess` + `overwriteMedia`; `exportToLibrary` returns `{uri,path,displayName}` (legacy `addToMediaStore` still returns the uri string for Workbench).
> - Player: `AudioPlayer(audioPipeline: PlaybackFx.createPipeline())`; EQ/ReplayGain/fades in `PlaybackFx`; transport for handler/widget/mini-player goes through `AudioService.resume/pause/nextTrack/prevTrack` (video-aware). `autoMountMusicFolder` removed — the Library tab replaces it.
> - Library tab = MediaStore (`storage.queryAudio`, `loadArtwork`), Pipeline tab labelled BATCH (`BatchItem`, REVIEW bucket).
> - Grabber: `bridge.py` finished events carry `info`; `cancel(job_id)`, `fetch_thumbnail`; `extractor_args` removed (list form was silently ignored by yt-dlp).
> - Playback Engine screen: fades + ReplayGain (crossfade/gapless toggles and the FFmpeg-only gapless audit were placebo and are removed).


## App identity

Sovereign Tagger — autonomous Android media pipeline: yt-dlp (chaquopy Python 3.14), FFmpeg DSP (`ffmpeg_kit_extended_flutter 0.6.2 full+gpl`), ACRCloud fingerprinting, Genius spider, ID3 jaudiotagger, `audio_service` lockscreen notification, PCM lossless `AudioRecord` wav, TapFeedback boops, Whisper AI transcription (`ggml-base.en.bin` 141MB bundled), **ReplayGain scanner (EBU R128 → TXXX tags)**, **15-band anequalizer (AutoEq-compatible, 25Hz–16kHz)**, **Crossfade (0–12s) + Gapless toggle**, **Gapless Audit test**, **LRCLIB lyrics fallback**, **Home screen widget (RemoteViews)**, **Library tab (MediaStore browse)**, **Backup/Restore (XOR encrypted JSON)**, **Cyberpunk Tap Feedback (particle burst + shockwave + haptic + 880/1400Hz audio)**, **Ghost Avatar Tutorial Chatbot (code-drawn glitch ghost, three-emotion particles, OverlayEntry, pure-Dart TF-IDF classifier — no native dep)**, **Contextual Settings Split (PlaybackEngineScreen via Player, EqPresetsScreen via Workbench — jetAudio pattern; root Settings = kernel/system only)**. Cyberpunk, accent-color themable, zero-bloat (no `test/`).

## Architecture map (verified)

### Dart layer (`lib/`, ~10.3k LOC across 23 files)

- **main.dart** (236 L): loads `accent_color` → `SplashScreen` (`FFmpegKitExtended.initialize` + XOR `0x53` auto-import + `MatrixRain` overlay `lib/widgets/matrix_rain.dart:50`) → `MainShell`. Boot introspection logs `FFmpegKitExtended.getRegisteredFilters()` count + `WHISPER SURFACE: DETECTED/NOT EXPOSED` verdict.
- **screens/main_shell.dart** (1091 L): `SovereignState` bus (`currentTab`, `pendingForgePath`, `accentColor`); `AudioService` owns `just_audio` `player` (0.10 playlist API via `_loadSources`/`setAudioSources`), `playlist`/`currentIndex`/`videoController`/`repeatMode`/`isShuffle`/`sleepTimerMinutes`/`speedFactor` + `SovereignAudioHandler` (`lib/core/audio_handler.dart`) + `_queueLock` serialized mutators (`replacePlaylist`, `reorderPlaylist`, `removeFromPlaylist`, `playNext`, `addToQueue`, `moveAfterCurrent`). `Stack` → `AmbientBackdrop` variant per tab + `IndexedStack` + mini-player + `BottomNavigationBar`. `_readTags` also `updateNowPlaying` handler. Queue-end now pauses + rewinds current track (keeps loaded state); `playIndex(currentIndex)` recovers from `completed`. **GhostChatOverlay** integrated via `GlobalKey`, positioned bottom-right above bottom nav.
- **tabs/tab_grabber.dart** (866 L): search/format-fetch/download with `_newJobId()` UUID, `FFmpegExecutor` queue, 3-rung merge ladder (`-map 0:v:0 -map 1:a:0` + AAC-remux + default) verified via `probeStreams`, silent-video self-heal (`_selfHealAttempts` capped at 1 + `companionJob` UUID). `videoController` preview `networkUrl`. **EXPORT AS** toggle: `MP3 320K` (default) / `SOURCE COPY` (webm/opus/m4a passthrough, zero transcode). Bottom sheets `Material(color, shape:)` fix. Post-tag `probeDurationMs` drift check (>2s) → lossless `-c copy -write_xing 1 -id3v2_version 3` repair + re-verify.
- **tabs/tab_forge.dart** (632 L): tag editor with `EJECT MEDIA` cache-only delete + confirm dialog `112`, export preserves container `267` (`mp3/flac/wav/m4a/ogg/opus`), write→read-back round-trip verify `372`, `PcmRecorder` not used here. Karaoke launches isolated `LyricSyncScreen` player (no global hijack) with `play/pause/restart` bar + `replaceAll('.mp3','')` → proper ext strip. Export purges staging copy from `Android/data/com.sovereigntagger/` or cache; status reports "Staging Copy Purged". Status box moved to TOP with compact padding.
- **tabs/tab_pipeline.dart** (560 L): batch with `GHOST/PARTIAL/PRISTINE/PROCESSED/FAILED` buckets, `cancel` button halts `for (… if(!_isBatchRunning) break)`, per-item `FAILED→GHOST` retry, `finally` wav cleanup `201`, `mounted` guards per iteration + outer `PlatformException` around `FilePicker`. **MIXTAPE JOIN**: lossless concat demuxer `-c copy -write_xing 1` of PROCESSED mp3s → MediaStore (`_mixtapeJoin`). Status box already top.
- **tabs/tab_workbench.dart** (1445 L): DSP via `FFmpegExecutor`, `probeDurationMs` for progress, mastering panel (`MASTERING QUALITY` `Source/44k/48k/96k soxr` + `16/24-bit` for `wav/flac`), `TWO-PASS LOUDNORM` (`_parseLoudnormMeasurements` regex), `Apply Fades`/`Fold To Mono`/`Trim Silence`; clip `HH:MM:SS` regex + `end>start` + `durationMs` guard + keyframe-aware `-ss` before `-i` for video. Recorder toggle `48k/16-bit WAV` via `PcmRecorderBridge` (default lossless `true`) vs `aac 48k/256k` fallback. **New ops (Phase 18)**: AI Denoise (`afftdn=nf=-25`), Parametric EQ (5-band ±12dB), Compress Dynamics (`acompressor` ratio 1-12:1). DSP pitch modes: VARISPEED / RUBBERBAND / ATEMPO dropdown. Normalize modes: TWO-PASS LOUDNORM / DYNAMIC `dynaudnorm` + **SCAN LUFS** (`ebur128`). **Whisper** (`_executeWhisper`): extracts 141MB model to cache, runs `-af "whisper=model=<path>:language=<en|auto>" -f srt`, writes `.srt` sidecar + text preview. Self-diagnosing fallback pulls `-h filter=whisper` into `_whisperReport`. HALT auto-exports recording (PCM + AAC) to Music via MediaStore immediately. Status box moved to TOP. **Phase 22**: **Scan ReplayGain (EBU R128)** — runs `ebur128` filter, parses integrated loudness + true peak, calculates track/album gain/peak, writes `REPLAYGAIN_TRACK_GAIN`, `REPLAYGAIN_TRACK_PEAK`, `REPLAYGAIN_ALBUM_GAIN`, `REPLAYGAIN_ALBUM_PEAK` as TXXX frames via native `id3` channel (jaudiotagger). **15-Band Equalizer (AutoEq)** — 15 bands (25, 40, 63, 100, 160, 250, 400, 630, 1k, 1.6k, 2.5k, 4k, 6.3k, 10k, 16k Hz, Q=1.2), purple-accent UI with SAVE PRESET/FLAT, persists to `SharedPreferences`, syncs with `EqPresetsScreen` (Workbench header tune icon). **Crossfade/Gapless** — relocated Phase 25 to `PlaybackEngineScreen` (Player AppBar merge-icon): crossfade 0–12s slider + gapless toggle (default on), cyan-accent UI.
- **tabs/tab_library.dart** (332 L): MediaStore browse by ALBUM/ARTIST/FOLDER dropdown, real-time search filter, tap track → replaces queue → switches to PLAYER tab, artwork from base64 tags.
- **tabs/tab_player.dart** (1099 L): Theater mode with pinch-zoom artwork (1→3x, `BoxFit.contain`), mini-player, `_SeekSlider` (drag local, seek on end), LRC karaoke, sleep/speed/shuffle/repeat, queue persist, lockscreen controls. **Phase 25**: AppBar actions — tune icon → `EqPresetsScreen`, merge icon → `PlaybackEngineScreen`.
- **screens/settings_screen.dart** (772 L, Phase 25 slimmed from 966): root = KERNEL/SYSTEM ONLY — XOR `0x53` keys (Genius/ACR), export/import `.bin` via `storage` channel, `CHECK FOR YT-DLP UPDATE` (`updateCore`), `FULL STACK REFRESH` (`updateFullStack` yt-dlp+mutagen/lyricsgenius), `ON-BOARD PYTHON DOCTOR` (`runDoctor` → `PATCH_REGISTRY` hits + inventory), FFmpeg probe card with **DOCS** button, cache purge, Ghost Tutorial (visibility/auto-expand/reduce-motion/reset first-launch), Backup/Restore (still reads/writes `crossfade_duration`/`gapless_enabled`/`eq15_band_$i` prefs directly). **Phase 25**: 15-Band EQ Presets + Playback Engine (crossfade/gapless/gapless-audit) sections MOVED OUT to dedicated screens.
- **screens/playback_engine_screen.dart** (182 L, Phase 25): Crossfade 0–12s slider (`onChangeEnd` persists) + Gapless toggle (persists on change) + Gapless Audit (2-tone 440/880Hz pattern → RMS gap measurement) via `FFmpegExecutor`. Cyan accent. Entry: Player AppBar merge-icon action.
- **screens/eq_presets_screen.dart** (~200 L, Phase 25): 15-band preset rack — **big VERTICAL sliders** (`RotatedBox` rails 260px tall, 52px bands, thumb r11) in horizontal-scroll AutoEq layout, ±12dB Q=1.2, freq label + dB badge per band, SAVE PRESET + FLAT, `_dirty` guard → PopScope discard-confirm dialog, syncs with Workbench via `eq15_band_$i`. Entry: **Player AppBar tune icon** (relocated off Workbench header per operator).
- **screens/lyric_sync_screen.dart** (292 L): own `AudioPlayer _player` (isolated, 48k), `play/pause/restart` bar + `H:MM:SS` position, tempo warp slider, `replaceAll` fix + `mounted` guards on `ScaffoldMessenger`.
- **shared helpers:** `lib/core/media_probe.dart` (`probeStreams`/`probeDurationMs` via `FFprobeKit.getMediaInformationAsync` → `s.type`), `lib/core/ffmpeg_executor.dart` (serialized `Queue<_Job>` → `FFmpegKit.executeAsync` with `onStatistics` → `onProgress`), `lib/core/tap_feedback.dart` (haptics + embedded 880/1400Hz wav base64 via `just_audio` data URI + `dispose()`), `lib/core/cyber_tap_feedback.dart` (**CyberTapFeedback** — particle burst 12 dots, expanding shockwave ring, button scale pulse; `CyberButton`/`CyberIconButton` convenience; `CustomPainter` reuse, zero alloc during anim), `lib/core/pcm_recorder.dart` (`PcmRecorder` MethodChannel + `amplitudes()` EventChannel stream `pcm_events` — live 50ms-throttled peak ints), `lib/services/ghost_classifier.dart` (**GhostClassifier** — pure-Dart TF-IDF + cosine similarity, 15 intents, no native dep, ~5KB, threshold 0.14). **Dead scaffolding removed**: `lib/core/jobs`, `lib/core/models`, `lib/services/ghost_tflite_service.dart`.
- **widgets:** `lib/widgets/matrix_rain.dart:50` `MatrixRain` (single `TextPainter` reuse, 36 cols) + `AmbientBackdrop` (`BackdropVariant` `grabber` vertical / `forge` slow / `pipeline` horizontal / `workbench` mixed, `alpha 0.035` grid + `0.18` sparks, 4s `AnimationController`). **Phase 19 upgrade**: electric arcs (jagged polylines, white-hot head + glow, 45%-cycle flashes) + traveling pulse comets (multi-dot tails gliding grid rows/cols). Per-variant counts (grabber 2/2, pipeline 2/3, forge 1/1, workbench 2/2). Lifecycle observer pauses on app pause/hidden. **Phase 24**: `lib/widgets/ghost_avatar.dart` — `GhostAvatar` (code-drawn Android ghost emoji silhouette, glitch effects: scanlines/RGB shift/jitter, glitch laugh wobble+particle burst+"hahaha" typewriter), three-emotion particle system (sarcastic/happy/serious weighted 72%), OverlayEntry in MainShell bottom-right, 72px collapsed orb, GhostSettings ValueNotifiers (reduceMotion/visible/autoExpand/firstLaunchComplete), reduce motion global disables glitch. `lib/widgets/ghost_chat_overlay.dart` — `GhostChatOverlay` (typewriter text, message bubbles, suggestion chips, text input, first-launch sequence "Welcome to... The Machine... SkyNet? hahaha!", contextual keyword tutorials per tab).

### Native layer (`android/app/src/main/`)

- **MainActivity.kt** → **`AudioServiceActivity`** (fixes `IllegalStateException` from audio_service plugin; lockscreen controls now functional). Channels: `ytdlp` (`searchMedia`/`getFormats`/`downloadMedia`/`cancelDownload`/`updateCore`/`updateFullStack`/`runDoctor`/`getDoctorRegistry`), `ytdlp_events` EventChannel (`jobId` UUID), `id3` (`writeTags` now `deleteField` on empty, `deleteArtworkField`), `storage` (`getTempDirectory`/`addToMediaStore` now MIME map `mp3→mpeg, flac→flac, wav→wav, m4a→mp4, ogg→ogg, opus→opus`), `acrcloud`, `spider`, `pcm_recorder` (`hasPermission`/`startRecording`/`stopRecording` → `PcmRecorderBridge.kt` `AudioRecord` 48k/16-bit WAV, `ByteBuffer` reuse, header finalize LE), `doctor` via `ytdlp` channel. **ACR fix**: bridge stores credentials, auto-reinitializes per call, fully stops+releases client after every recognition — mic indicator extinguishes immediately.
- **Python** `android/app/src/main/python/sovereign_yt/`: `bridge.py` (`"vid+aud"` split into two `YoutubeDL` runs, `extractor_args player_client=android,web`, jobId templated `"{job_id}_v/a.%(ext)s"`, split orphan cleanup), `spider.py` (Genius→**LRCLIB**→SoundCloud→iTunes→MusicBrainz cascade, LRCLIB free lyrics no key), `updater.py` (`execute_scorched_earth` tarball + `update_python_stack` best-effort pip + `execute_full_scorched_earth`), `doctor.py` (`PATCH_REGISTRY` 5 signatures, `diagnose()`). `train_ghost_intent.py` removed — TFLite never had Python 3.14 wheels; pure-Dart classifier planned instead.
- **Id3Tagger.kt**: Added ReplayGain TXXX frame support — `setReplayGainTag`/`getReplayGainTag` handle `REPLAYGAIN_TRACK_GAIN`, `REPLAYGAIN_TRACK_PEAK`, `REPLAYGAIN_ALBUM_GAIN`, `REPLAYGAIN_ALBUM_PEAK` via `FrameBodyTXXX`/`ID3v24Frames`.
- **SovereignWidgetProvider.kt** — Home screen widget (RemoteViews), 4-button control (PREV/PLAY/NEXT/QUEUE).

## Known-issue registry (all gated `No issues found!`)

| ID | Site | Original issue | Phase | Status |
|----|------|---------------|-------|--------|
| K1 | `grabber:184` | literal `$_quickVideoRes` | 1 | **FIXED** `"$..."` |
| K2 | `pipeline:66` | unguarded `getTempDirectory` → batch lock | 1 | **FIXED** `try/catch` + `if(!mounted)` |
| K3 | `player:moveAfterCurrent` | `playlist` mutated w/o `_audioSource` | 1→10 | **FIXED** `await move/insert` inside `_withQueueLock` + `UUID` |
| K4 | `forge:121` | eject deleted original | 1 | **FIXED** cache-only `deleteSync` + confirm dialog |
| K5 | `forge:267` / `pipeline:267` | export hardcoded `.mp3` | 1 | **FIXED** `srcExt` preserve `mp3/flac/wav/m4a/ogg/opus` |
| K6 | FB muxing | silent video | 2 | **FIXED** `probeStreams` + 3-rung ladder + self-heal capped 1 |
| K7 | `×9 sites` | sync `FFmpegKit.execute` freeze | 3 | **FIXED** all `FFmpegExecutor.execute` queued |
| K8 | player | no lockscreen | 4→10 | **FIXED** `audio_service 0.18.19` `SovereignAudioHandler` + manifest service |
| K9 | forge | no verify; karaoke hijack | 5 | **FIXED** read-back mismatch + isolated `AudioPlayer` + transport bar |
| K10 | workbench | `loudnorm→mp3` | 6 | **FIXED** container-preserving `flac→flac, wav→wav` + loudnorm preserved |
| K11 | pipeline | no cancel/retry | 7 | **FIXED** cancel halts `for` loop, per-item `FAILED` retry |
| K12 | splash/shell | static bg | 8 | **FIXED** `MatrixRain` + per-tab `AmbientBackdrop` pulses |
| K13 | global | ~15 missing `mounted` | 9→post | **FIXED** `forge:85,180,276`, `grabber:201,226`, `pipeline:52,66`, `workbench:110`, `main_shell:72,601`, `PcmRecorderBridge:60` reuse |
| A1 | player | mini-player "System Idle" while playing | 17 | **FIXED** `hasMedia` no longer clobbered, `completed→stopPlayer` gated |
| A2 | player | slider seek lag | 17 | **FIXED** `_SeekSlider` StatefulWidget, drag local only, seek on `onChangeEnd` |
| A3 | grabber | MP3 phantom 32kbps/44:12 metadata | 17 | **FIXED** post-tag `probeDurationMs` drift check >2s → lossless repair |
| A4 | player | album art side-cropping | 17 | **FIXED** theater artwork `BoxFit.cover`→`contain` |
| A5 | settings | 164MB user-data purge incomplete | 17 | **FIXED** `_purgeCache` sweeps `Android/data/com.sovereigntagger/files` staging |
| V1 | grabber | video merge panic (ffprobe `{}` on fresh MKV) | 21 | **FIXED** retry probe once after 400ms, trust return code on unknown |
| V2 | player | "NO AUDIO LOADED" + missing art at queue end | 21 | **FIXED** queue end pauses+rewinds, keeps loaded state |
| V3 | acr | mic indicator stuck on | 21 | **FIXED** bridge fully stops+releases ACR client per call |
| V4 | player | queue-sheet ListTile assertion | 21 | **FIXED** `_showQueueSheet` Container → `Material(type: transparency)` |
| RG1 | workbench | ReplayGain scanner | 22 | **IMPLEMENTED** `ebur128` → TXXX tags via id3 channel |
| EQ1 | workbench/eq_presets_screen | 15-band anequalizer | 22→25 | **IMPLEMENTED** 15 bands (25Hz–16kHz, Q=1.2), AutoEq-compatible; preset UI relocated to `EqPresetsScreen` (P25), Workbench live editor unchanged |
| XF1 | settings/player | Crossfade + Gapless | 22 | **IMPLEMENTED** 0–12s crossfade slider + gapless toggle (default ON), persisted |
| GA1 | settings | Gapless audit test | 22 | **IMPLEMENTED** 2x 1s sine tones (440/880Hz) → concat → RMS gap measurement |
| LR1 | spider.py | LRCLIB lyrics fallback | 22 | **IMPLEMENTED** Free API (no key), synced+plain lyrics, after Genius in cascade |
| HW1 | widget | Home screen widget | 22 | **IMPLEMENTED** `SovereignWidgetProvider` (RemoteViews), 4-button control |
| LB1 | tabs/tab_library | Library tab | 22 | **IMPLEMENTED** MediaStore browse by Album/Artist/Folder, search, play |
| BR1 | settings | Backup/Restore | 22 | **IMPLEMENTED** XOR `0x53` encrypted JSON: settings + EQ + queue |
| CT1 | core | Cyber tap feedback particle burst | 23 | **IMPLEMENTED** 12 dots, pre-computed trajectories, zero alloc |
| CT2 | core | Cyber tap feedback shockwave ring | 23 | **IMPLEMENTED** expanding ring thins + inner glow pulse |
| GH1 | widgets/ghost_avatar | Ghost Avatar glitch ghost | 24 | **IMPLEMENTED** code-drawn Android ghost emoji, scanlines/RGB shift/jitter, glitch laugh |
| GH2 | widgets/ghost_chat_overlay | Ghost Chat Overlay | 24 | **IMPLEMENTED** typewriter, bubbles, chips, first-launch SkyNet joke |
| GH3 | services/ghost_classifier | Intent classifier brain | 24→C | **IMPLEMENTED (pure-Dart)** TF-IDF + cosine, 15 intents, threshold 0.14 — original TFLite lane ABANDONED (JVM-target 1.8 vs 25 fatal) |
| GH4 | ghost_chat_overlay routing | Brain routing order | 24 | **IMPLEMENTED** hide/laugh short-circuits → classifier → keyword fallback |
| GH5 | core/ghost_settings | Ghost prefs | 24 | **IMPLEMENTED** visible/autoExpand/reduceMotion/firstLaunchComplete persisted; useTflite removed |
| GH6 | settings | Ghost Tutorial section | 24 | **IMPLEMENTED** visibility, auto-expand, reduce-motion, reset first-launch (TFLite toggle + train button ripped with lane C) |
| CS1 | screens/playback_engine_screen | Playback Engine contextual screen | 25 | **IMPLEMENTED** crossfade/gapless/gapless-audit relocated from Settings; Player AppBar entry; instant persist |
| CS2 | screens/eq_presets_screen | EQ presets contextual screen | 25 | **IMPLEMENTED** 15-band matrix relocated from Settings; Workbench tune-icon entry; dirty-guard PopScope; syncs `eq15_band_$i` |
| CS3 | screens/settings_screen | Root Settings = KERNEL/SYSTEM only | 25 | **IMPLEMENTED** 966→772 L; keys/accent/maintenance/ghost/backup only |
| GH7 | ghost_chat_overlay | Ghost never rendered on device | 25 | **FIXED** nested `Positioned` (overlay returned its own Positioned inside MainShell's) → ParentDataWidget crash killed whole subtree; overlay now returns plain Column |
| GH8 | ghost_avatar:613 | Orb still invisible — AnimatedOpacity assert every frame | 25 | **FIXED** easeOutBack/easeInBack drove `_opacity` outside [0,1]; clamped opacity+scale at widget. Flag bumped to `ghost_first_launch_complete_v2` (earlier builds consumed it invisibly); MainShell reads via GhostSettings |
| GH9 | main_shell Stack z-order | Ghost rendered UNDER opaque tab Scaffolds — invisible despite healthy build/sounds/IME | 25 | **FIXED (root cause)** ghost was Stack child #2 under the tabs' `Column`; moved to LAST child so it paints on top. Auto-keyboard at launch also removed (status-listener focus steal) |
| GH10 | ghost_avatar `_drawGhost` | Orb invisible — path drawn off-canvas, app looked like "a box with text" | 25 | **FIXED** `ghostPath.shift(center)` translated instead of scaled the 100-unit design space → 75% off-canvas. Now translate-to-center + `scale(shortestSide/100)` + recenter on bounds (51,57). Plus: orb is drag handle (overlay movable), auto-collapse to orb 12s post-tutorial |
| GH11 | ghost_avatar/overlay lifecycle | Orb scale-0/opacity-0 forever: materialize one-shot raced collapsed-shrink → null currentState silent no-op; collapse destroyed state; flag key desync | 25 | **FIXED (final root cause)** avatar self-materializes post-frame on mount (silent); overlay never returns shrink — orb always mounted/tappable when visible; overlay reads v2 flag via GhostSettings; agent audit F1-F10 confirmed painter was never at fault |
| CS4 | tab_player / eq_presets_screen | EQ belongs in PLAYER (jetAudio pattern), not Workbench | 25 | **FIXED** tune icon moved to Player AppBar; vertical 260px slider rack built; Workbench keeps DSP op + SAVE PRESET sync |
| PW1 | workbench PCM | static "PCM CAPTURING" indicator | 25 | **FIXED** `_PcmWavePainter` live 48-bar level meter fed by `PcmRecorderBridge` peak tap → `pcm_events` EventChannel (50ms throttle, zero extra alloc in read loop) |

## Toolchain notes (dependency ceiling: win32 pair + compileSdk 37)

- Phase 27.1 resolver audit: `ffmpeg_kit_extended_flutter 0.6.2`, `just_audio 0.10.6`, `audio_session 0.2.4`, `audio_waveforms 2.0.2`, `permission_handler 12.0.3`, `flutter_lints 6`, `flutter_launcher_icons 0.14.4`, `video_player 2.14.0`, `audio_service 0.18.19` are all at latest. Capped: `file_picker 9.2.3` ↔ `wakelock_plus 1.5.2` (win32 ^5 vs ^6) and `permission_handler` 13 (its Android side needs compileSdk 37; root gradle forces 36). The old claim that ffmpeg 0.6 / just_audio 0.10 were win32-blocked was wrong. `just_audio_background` Beta unresolvable — replaced by `audio_service` handler already wired.
- `android/build.gradle:13` `chaquopy 17.0.0` supports `AGP 7.3-9.2 / Python 3.10-3.14`. `python 3.14` is top but few wheels — fallback comment `app/build.gradle:54` says flip to `"3.11"` if `pip` fails. `3.12+` is 64-bit only (`arm64-v8a,x86_64` only). `compileSdk 36 / NDK 28.2` / `JDK 25 bundled` (`flutter doctor -v` `jbr/bin/java 25.0.2`) with `compileOptions 1.8` shim — bump to `17` only in dedicated Java session.
- `android/app/build.gradle:62` release signing now conditional on `SOVEREIGN_KEYSTORE` env/`local.properties` else `debug` fallback (Play Store still needs real keystore). `abiFilters arm64-v8a,x86_64` documented (64-bit-only saves ~30% APK).
- `AndroidManifest.xml:17` now `READ_MEDIA_AUDIO/VIDEO/IMAGES` + `WAKE_LOCK` + `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MEDIA_PLAYBACK` + `AudioService`/`MediaButtonReceiver`.
- **Device probe verdict (operator-run):** FFmpeg 94138f6 (built 20260719), **548 filters registered**, **WHISPER SURFACE DETECTED**, pro libs confirmed: rubberband, soxr, x264, vpx, opus, vidstab.
- **Repo cleaned**: Removed dead scaffolding (`lib/core/jobs`, `lib/core/models`, `tools/`), stale blueprints (old versions, PDFs, large text dumps).
- No `test/` suite (deferred-zero-bloat); verification is `flutter analyze --no-pub` + device smoke.

(End of file - total 72 lines)